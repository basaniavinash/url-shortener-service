apiVersion: v1
kind: Service
metadata:
  name: shortener
  namespace: url-shortener
spec:
  type: ClusterIP
  selector:
    app: shortener
  ports:
    - name: http
      port: 80
      targetPort: 8000
